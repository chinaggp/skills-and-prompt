```
## 一、多subagent协作

```

[需求]
这是一个 Java 项目需求，请按以下协作规则执行：

1. 默认只使用 explorer、worker、reviewer。
2. 仅当需求涉及外部文档、版本差异、第三方 API 或规范不明确时，才增加 docs_researcher。
3. explorer 负责只读调查，输出入口、调用链、推荐改动点、测试落点。
4. worker 负责最小范围实现和补测试，优先复用现有代码结构。
5. reviewer 负责只读复审，重点检查正确性、边界条件、回归风险和测试覆盖。
6. 所有输出使用中文，尽量精确到文件路径、类名、方法名。
